# G4 R03 weather execution — verification still required

Latest user request: implement the PROJECT, not generated images; add actual rain, refined realistic materials, realistic ocean and mountain/cliff.

The previous generated exterior/interior pictures are concept images only. They are not Blender outputs and must not be used as scene-edit, render, animation or acceptance evidence.

## Execution entry submitted in this turn

- `enhance_g4_weather_r03.py`: guarded native scene build and renders from the exact saved R02.
- `execute_g4_weather_r03.py`: explicit field-evaluation correction and original glass-camera witness; records executed source bytes.
- `fetch_g4_rock_reference.py`: bounded optional retrieval of one actually selected official CC0 rock texture family with source/hash records.
- `.github/workflows/glasshouse-g4-weather-r03.yml`.
- `g4-weather-r03-request.json`: one public standard runner, serial production, job60 minutes / production52 / reserve8, external spend0. Do not edit the completed request to trigger another run blindly.

Planned output root: `workspaces/glasshouse-terminus/output/g4-r03-weather`.
Planned master filename: `g4-weather-scene-candidate.blend`.

These paths identify submitted source and intended outputs, not a claim that every output currently exists. No new run ID, master hash, completed artifact or art pass has been verified in this record. First read live GitHub Actions for the exact workflow, then inspect its real logs and returned evidence. If it failed, diagnose the actual exception before any bounded recovery; do not rebuild from G2 or submit duplicate jobs.

## Last fully established parent

R02 evidence commit `12ae1030686d97823548b2967805d85853d12ab2`.
`workspaces/glasshouse-terminus/output/g4-r02/g4-full-scene-candidate.blend`.
SHA-256 `173da1291bff8a39da704539c737e21f9b2d739e3d32a106f629601bc2abefdc`.
Previous G4 state/reviews remain historical verified evidence, not evidence of R03 completion.

## Actual requested implementation and gates

Native Blender Ocean modifier with saved time driver and foam; distant sea continuation instead of the finite flat cube; actual stratified cliff geometry retaining the existing top support interface; geometry-node rain with actual shelter-ray masking; water relief on existing glazing; separated outdoor wet paving and indoor dry stone; preserved original camera transforms, exposure, motion, original node lighting and prior checkpoints.

The source performs an actual data inventory and asserts expected source geometry and hash before permitted modifications. If assumptions differ, it must stop rather than force a rewrite. Optional rock downloads cannot be claimed unless `assets/rock-sources.json` and actual verified bytes exist.

Required evidence: original-camera native hall/exterior renders, ocean/rock/glass closeups, original D01 regression, fresh-process source checks, and separately labelled native short motion witness. Native pixels must actually be inspected. Success of a workflow or presence of a modifier is not art acceptance. A generated picture cannot substitute for any missing render.

`g3_historical_pass=false`, `g4_stage_pass=false`, `human_acceptance=false`. G4 stage-order exception remains user-authorized; do not reinstate a stale G3 gate as a reason to refuse all G4 work. Preserve G2 BEST, R07, R01/R02 and all failure evidence. main/Leaf unchanged. Browser administrator restriction unchanged and not bypassed. Final film and website remain uncompleted.
