# G2 原图审查 01 — 已恢复工件，不提前放行 G3

日期：2026-09-07。评审：同上下文实际图像自审，不是独立代理或用户验收。

## 已读取的真实证据

父制作源码：`a40810d2861a2582499db410c6b41881f1199d18`；Actions `34106817235`。
固定视图 artifact：`10013204369`，10,648,118 bytes，ZIP SHA-256 `beeef842ddeab0dcb87d35013fb8f670a34221d98e351437ac4ae137f0aac22d`，下载后校验匹配。
持久副本：`evidence/g2-fixed-34106817235-1`，commit `453a74c1d0eb668ed483219eb684bf57798d4a8c`。
完整白模：`workspaces/glasshouse-terminus/output/g2/g2-complete-whitebox.blend`，SHA-256 `ef6fa3dd7e291564f2d1c158cb002f07dcf05c071c6f72e34e95c68ca2a22bf0`，由持久 manifest 核实；尚不把文件存在当艺术通过。

本次逐一打开 C01–C09 的 1280×800 原图，另看联系表。不是只读缩略图或日志。固定集为 24 samples、CPU Cycles、中性 QA 照明；不是最终雨夜 Beauty。九图记录的实际渲染总和约 695.24 秒，不外推成 4K 成片预算。

## 观察

C02 反面有完整端墙、玻璃壳、基座与室内物体；C03/C04/C06 的站房门洞、站台与车门关系可以互相识别。C08 剖面显示厅、站台与车厢的真实体积和高度关系；C09 有完整座位、过道、顶面及驾驶区，不能把项目描述成尚未建模。家具与植物仍为白模代理，此阶段不按最终精修资产判定。

### G2-F01 / MAJOR / 主构图未冻结

C01 左侧画框裁掉站房端部；不能用这个角度证明完整主轮廓。候选干预：只扩大外部主机位的画幅覆盖，保留站房、轨道、镜头朝向及曝光。先保留旧 C01，再提交同机位修正对照；校核主片 0–3 秒的整座站房边界。不得用改镜头来隐藏桥体缺陷。

### G2-S01 / MAJOR 待诊断 / 桥体接合

C01 桥拱落到桥墩处出现黑色楔形接缝及错向台阶，连接关系不够可信；C07 的相邻跨端部也有重叠轮廓。源码逐跨同时创建两端桥墩/基础，内部共享节点被创建两次，且分别沿相邻弦段旋转，这是已找到的原因候选；尚不能只凭代码证明全部黑边的成因。

候选干预：新进程核验实际对象分组与父工件 SHA 后，每个共享节点保留一套沿轨道局部切线的支撑；调整墩柱与基础的接触，补一个真实承接拱脚的支承帽。附同一近景前/后实渲、原 C01 机位的修正实渲，并重渲 C01–C09。若对象布局不符立即停止，不强行套用。

## 阶段决策

当前 `G2_REVIEW_UNMET`。完整白模已有可恢复 checkpoint；尚未建立 G2 BEST，G3 不放行。28 秒白模预演与实际路径/地板/车门检查仍待本轮成品取回后审查，本记录不把动画列为通过。

本轮只进行一次有界 G2 空间/构图修正：公开标准 runner、单并发、job ≤60 分钟、外部支出零、下载 ≤1.5 GB、工作盘 ≤8 GB、工件回传 ≤180 MB。按当前吞吐计划约 35–40 分钟制作/预演，至少预留约 15 分钟保存、取回和判断；到界保存未通过工件，不追加无诊断重跑。

G1 的 P1 浏览器 `BLOCKED_BY_ADMINISTRATOR` 保持不变；不清除认证、不改安全配置、不换浏览器规避。最终 4K 主片、短版、证据片、最终质量样板和同源网页均不在本次白模完成声明中。
